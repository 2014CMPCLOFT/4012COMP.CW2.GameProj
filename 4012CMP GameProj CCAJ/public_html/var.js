//Variables to be loaded first before any other script. 
//All scripts will be merged into one for release. This is just to make it
//tidier.

    //var canvas; //Not used?
    var ctx;
    //var WIDTH = 650;
    //var HEIGHT = 850;
    var intervalId = 0;
    var container = 'game';
    /*
              var imageObj = new Image();
                imageObj.onload = function() { draw();
                };
                imageObj.src = 'cloud.png'; 
                
            var bulletimg = new Image();
                bulletimg.onload = function() { draw();
                };
                bulletimg.src = 'Bullet.png'; 
          
            var landimg = new Image();
                landimg.onload = function () { draw();
                };
                landimg.src =  'Land-place-holder.png';

*/